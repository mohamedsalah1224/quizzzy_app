import 'package:flutter/material.dart';

class ExamView extends StatelessWidget {
  const ExamView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Exam View");
  }
}
